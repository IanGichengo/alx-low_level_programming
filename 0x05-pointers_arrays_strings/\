#include "main.h"
#include <string.h>

/**
 * print_rev - print in reverse
 * @s: parameter
 */

void print_rev(char *s)
{
	int len = strlen(s);

	while (len--)
		_putchar(*(s + len));
	_putchar(10);
}
