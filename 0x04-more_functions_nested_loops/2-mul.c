#include "main.h"

/**
 * mul - function to multiply two integers
 * @a: one of the integers
 * @b: one of the integers
 * Return: retruns zero to show code is right
 */

int mul(int a, int b)
{
	return (a * b);
}
