#include "main.h"
#include <stdio.h>

/**
 * main - starting point
 * @argc: count
 * @argv: array of character
 * Return: result
 */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);

	return (0);
}
